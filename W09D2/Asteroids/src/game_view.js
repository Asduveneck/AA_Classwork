function GameView(options) {
  this.game = game;
  this.drawing = draw
}

GameView.prototype.start = function() {
  // calls setInterval
      // calls Game.prototype.moveObjects   
      // calls Game.prototype.draw   
  //
}

GameView.prototype.moveObjects = function() {
  // calls setInterval
}
